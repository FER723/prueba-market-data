import { ColorNumberDirective } from './color-number.directive';

describe('ColorNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorNumberDirective();
    expect(directive).toBeTruthy();
  });
});
